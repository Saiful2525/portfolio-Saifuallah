# Saiful Fullstack Portfolio Backend

Laravel REST API backend for fullstack portfolio showcase.

## Setup
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve

## API Endpoints
GET /api/profile
GET /api/projects
